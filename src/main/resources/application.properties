# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/demo
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

debug=true

logging.level.org.springframework.security=DEBUG
# OAuth2 Client Configuration
#security.oauth2.client.client-id=mobapp
#security.oauth2.client.client-secret=mobappsecret
#security.oauth2.client.authorized-grant-types=authorization_code,password,refresh_token
#security.oauth2.client.scope=read,write
#security.oauth2.client.redirect-uri=http://localhost:8080/callback
#security.oauth2.access-token-validity-seconds=30
#security.oauth2.refresh-token-validity-seconds=60